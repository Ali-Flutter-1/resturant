<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ replacement for allowBackup. Nothing this app stores is worth
  moving to another device: the session tokens are the sensitive part, and a
  restored copy of them on a phone the customer no longer controls is exactly
  what backup should not produce. Orders and the menu all come back from the
  API on the next sign-in, so excluding everything costs the customer nothing.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
